# Leave empty in development — Vite proxies /api to localhost:4000 automatically.
# In production this is set via client/.env.production (committed to repo).
VITE_API_URL=https://gradispace-production.up.railway.app/api/v1
